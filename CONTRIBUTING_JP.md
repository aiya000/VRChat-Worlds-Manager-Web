# VRChat Worlds Manager Web へのコントリビュート

[English CONTRIBUTING is here / 英語のCONTRIBUTINGはこちら。](./CONTRIBUTING.md)

VRChat Worlds Manager Web へのコントリビュートをご検討いただき、ありがとうございます。  
不具合の報告、機能の提案、翻訳、コードの改善など、どんな形のコントリビュートも歓迎します。

## 使用する言語

Issue と Pull Request は、**英語でも日本語でも**構いません。

この2つが挙がっているのは実務上の理由からで、それ以外の意味はありません。

- **英語** は、ここへ辿り着く人がいちばん読める言語だからです
- **日本語** は、メンテナの母語だからです

これは「誰を歓迎するか」を言っているものではありません。どちらも書きにくい場合は、好きな言語を機械翻訳にかけて書いてください。このリポジトリでは Issue の立て方としてごく普通のことですし、言葉づかいを理由に報告の価値が下がることはありません。

## このプロジェクトは、まるごと Vibe Coding で作られています

このリポジトリのコードは、すべて会話をもとに AI エージェントが書いたものです。一時的にそうしているわけではなく、これがこのプロジェクトの作り方です。

そこから2つのことが出てきます。パッチに一晩を費やす前に、お伝えしておくのが公平だと思います。

- **マージされたコードは、あとから誰にも断りなく書き直されたり、消されたりすることがあります。** 出来が悪かったからではありません。エージェントがある領域を作り直すときは、そこにあるものをまとめて書き直しますし、どの行がどこから来たかという記録もないためです
- **コミット履歴が書き換えられることはありません。** あなたの名前のついたコミットは、このリポジトリが存在するかぎりログに残り続けます。コードのほうが残らなくなっても、功績は永久にあなたのものです

## Pull Request の前に、必ず Issue を立ててください

**大きな変更にかぎらず、必ずです。**

何を足したいのか、何を直したいのかを書いてください。できれば、どう作るつもりかも書いてほしいです。設計、インターフェースの形、画面がどう見えるか。ここでは、文章を1段落書くよりも、ラフなスケッチやスクリーンショット1枚のほうが役に立ちます。

これをお願いしているのは、前の節の事情によります。コードは速く、かつ大きな範囲でまとめて動くので、理由を先に書かないまま出された Pull Request は、読まれるころには前提ごと追い越されていることが多いのです。そうなると、何と比べて良し悪しを判断すればいいのかも分かりません。Issue は、コードが残らなくても意図のほうを次の書き直しまで生き延びさせるためのものです。

## コントリビュートの方法

1. **リポジトリを Fork** して、`develop` からブランチを切ってください。開発は `develop` で行われています。`main` は本番リリース用です
2. Pull Request に、**変更内容を分かりやすく書いてください**
3. 提出する前に、**変更をテストしてください**

### Issue

コードに手を入れる前に、上記のとおり [Issue](https://github.com/aiya000/VRChat-Worlds-Manager-Web/issues) を立ててください。質問やトラブルシューティングも Issue で構いません。このリポジトリには Discussions がないためです。

ある Issue に取り組むと決めたら、作業の重複を避けるために自分をアサインしてください。
自分でアサインする権限がない場合は、その旨をコメントで残してください。

> [!WARNING]
> 新しい Issue を立てる前に、重複がないか確認してください。  
> また、実装方針が決まったとしても、コードが完全にマージされるまでは Issue を閉じないでください。

### Pull Request

Pull Request を作るときは、次のことを守ってください。

1. 関連する Issue があることを確認してください（なければ立ててください）
2. `main` ではなく `develop` を向けてください
3. レビューしやすいよう、変更は最小限にしてください
4. （できれば）ブランチ名に接頭辞を付けてください（例: `feat/`、`fix/`、`perf/`、`docs/`）
5. （できれば）UI の変更にはスクリーンショットを添えてください

言語ファイルに項目を追加した場合、翻訳をすべて自分で埋める必要はありません。  
Pull Request を作ったあとで、翻訳のコントリビュートを募ることができます。

## 開発環境のセットアップ

次のものをインストールしてください。

- [Bun](https://bun.sh/)（v1.2 以上）

エディタは [VSCode](https://code.visualstudio.com/) をおすすめしますが、好きなものを使って構いません。

> [!IMPORTANT]
> **このプロジェクトが使うパッケージマネージャは Bun だけです。** `npm`、`yarn`、`pnpm` はいずれも `bun.lock` の隣に独自のロックファイルを書き、依存関係の解決も異なります。

### パッケージのインストール

```bash
bun install
```

Playwright が動かすブラウザはこれに含まれません。マシン全体で共有されるキャッシュに置かれ、Playwright のバージョンに紐づいています。最初に一度、そして Playwright を更新したときにもう一度、次を実行してください。

```bash
bunx playwright install chromium
```

### 開発モードで動かす

```bash
bun run dev
```

ポートが 3456 に固定されているのは意図的です。Google OAuth クライアントに登録済みのオリジンなので、ここを変えるとローカルで Google にサインインできなくなります。

フロントエンドのコードを変更すると、自動的に反映されます。  
反映されないときは、`Ctrl-R` で再読み込みしてみてください。

### チェックとテスト

```bash
# Prettier、ESLint、TypeScript の3つをまとめて
bun run check

# ユニットテストと統合テスト
bun run test

# E2E テスト（Playwright）。開発サーバーは自分で立ち上げます
bun run test:e2e

# 本番ビルド
bun run build
```

`worker/` 以下に手を入れた場合は、そちら側のチェックも実行してください。

```bash
cd worker && bun run check
```

### コードフォーマット

```bash
bun run format
```

### 便利なコマンド

GitHub で PR をマージしたあと、リモートのブランチは削除されても、ローカルのブランチは残ったままになることがあります。
散らからないよう、次の条件に当てはまるローカルブランチを掃除できます。

- すでにマージされ、GitHub 側では削除されている
- 以前 push したが、origin にはもう存在しない
- ローカルだけのプライベートなブランチではない

```bash
git fetch --prune
git branch -vv | grep ': gone]' | awk '{print $1}' | xargs -n 1 git branch -d
```

---

VRChat Worlds Manager Web をより良くするために、ありがとうございます。
